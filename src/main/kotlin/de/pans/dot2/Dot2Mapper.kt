package de.pans.dot2

object Dot2Mapper {

    fun map(bytes: List<Int>): List<Int> {
        val bytes = bytes.toMutableList()
//        if (Settings.isBound(bytes[1])) {
//            bytes[0] = 144
//            bytes[1] = Settings.getBind(bytes[1])
//
//            return bytes
//        }
        return emptyList()
    }

}